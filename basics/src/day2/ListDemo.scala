package day2

object ListDemo {
  def main(args: Array[String]): Unit = {
  //create a list of 5 Employees 
    val employees  = List( new Employee("shyam"), new Employee("Ram"))
    //sorting
    
//    val list1: List[String] = List("Ram", "Sam", "Jhoony", "Rakesh kumar")
//    println(list1)
    //    list1.foreach(item => println(item))
    //    val filtered = list1.filter(str => str.length() > 4)
    //less than 4 characters or greater than 6 chars
    //    print(filtered)
//    val sortedList = list1.sorted
//    println(sortedList)
  }
}